# Agent operating contract

Before changing this repository, read `docs/knowledge-base/manifest.json` and every listed file in order. Treat `docs/project/contracts/` and accepted ADRs as canonical. Update affected docs with code, run `npm run docs:check`, and never put secret values in docs or agent context.

Use NEAT queries and discovery for codebase search. Do not use grep or ripgrep.
