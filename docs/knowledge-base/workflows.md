# Workflows

Install with `npm install`, run `npm run hooks:install`, start Supabase with `npm run supabase:start`, copy `.env.example` to `.env.local`, and run `npm run dev`. Use `npm run docs:watch` while editing and `npm run check && npm run build` before handoff. Never run `supabase db reset --linked`.
