# Project

EdgeCase is a hackathon prototype foundation: a fast path from a Vercel-hosted Next.js frontend to Supabase Edge Functions and Postgres. Prefer the smallest reversible implementation that proves a user-visible path. Complex service boundaries and premature domain modelling are non-goals. `notes` is a placeholder domain.
