# Contracts

`http/openapi.yaml` defines Edge Function HTTP behavior, `database/schema.sql` is a review snapshot, and `env.md` defines variable names and visibility. OpenAPI and migrations take precedence over prose.
